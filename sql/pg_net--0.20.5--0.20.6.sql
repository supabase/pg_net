revoke update on all sequences in schema net to PUBLIC;

revoke insert on all sequences in schema net to PUBLIC;
revoke update on all sequences in schema net to PUBLIC;
revoke delete on all sequences in schema net to PUBLIC;
revoke truncate on all sequences in schema net to PUBLIC;
revoke references on all sequences in schema net to PUBLIC;
revoke trigger on all sequences in schema net to PUBLIC;
revoke maintain on all sequences in schema net to PUBLIC;
